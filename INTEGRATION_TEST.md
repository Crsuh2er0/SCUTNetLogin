# SCUTNetLogin 集成测试清单

> 面向实机（校园网）验证。单元测试已本地全量通过（47 用例，Qt 6.8.3 + MSVC 2022）；
> 本清单验证真实网络下的端到端行为，由测试人员在校园网环境执行并勾选。

---

## 0. 前置准备

### 0.1 环境要求

- [ ] Windows 10 / 11 x64
- [ ] Npcap 已安装（勾选 **Support raw 802.1X traffic**）—— 有线场景必需，无线 Portal 仅走 HTTPS
- [ ] 一个可用的校园网账号（注意 SCUT 并发限制：1 有线 + 2 无线在线，测试无线前先下线其他终端）

### 0.2 构建

```bat
rem Developer Command Prompt for VS 2022 (x64)
set PATH=D:\Program Files (x86)\Qt\6.8.3\msvc2022_64\bin;%PATH%
set NPCAP_SDK=D:\Program Files (x86)\npcap-sdk
cd /d C:\Users\16870\Documents\GitHub\SCUTNetLogin
qmake SCUTNetLogin.pro
nmake release
windeployqt --release --compiler-runtime release\SCUTNetLogin.exe
```

- 产物：`release\SCUTNetLogin.exe`（自包含 Qt DLL + VC143 运行库）
- 首次运行会弹 UAC 管理员确认（manifest 要求）

### 0.3 服务器地址

- [ ] 五山校区：`s.scut.edu.cn`（默认）
- [ ] 大学城校区：改为 `s2.scut.edu.cn`（设置 → 服务器）

---

## A. 无线 Portal（本次新增功能，优先验证）

### A1 配置持久化与 UI 联动

- [ ] 连接页选择「无线 Portal」→ 网卡/刷新/MAC/IP/掩码/网关/DNS/静态IP复选框全部禁用
- [ ] 保存配置 → 退出 → 重启 → 仍是「无线 Portal」且控件保持禁用（`authMode=wireless` 已落盘）
- [ ] 切回「有线 802.1X」→ 控件恢复可用

### A2 正常登录（已连 scut-student WiFi）

- [ ] 填账号密码 → 点连接
- [ ] 日志顺序：`正在查询 Portal 在线状态...` →（未在线）`正在登录 Portal（<本机IP>）...` → `Portal 认证成功，可以上网了！`
- [ ] 状态变「已连接」（绿色对勾），浏览器可正常上网（无重定向劫持）
- [ ] 服务器自签证书时首轮有一条 `Portal 服务器证书异常(...)已忽略校验` 警告（正常）

### A3 已在线兜底（多端/掉线重登竞态）

- [ ] chkstatus 显示已在线：直接 `账号已在线` 并连接成功，无 login 请求
- [ ] 另一设备登录同账号后本机再连：login 返回「…已经在线…」→ 按成功处理（状态已连接）

### A4 密码错误（永久性错误，不自动重试）

- [ ] 填错误密码 → 连接
- [ ] 期望：`Portal 认证失败: 密码错误`（红色）+ `该错误无法自动恢复，已停止自动重试`，停在「未连接」
- [ ] 等待 >5 分钟，确认**不会**反复自动重试
- [ ] 英文部署提示（如 `incorrect password`）同样应判定为永久性错误

### A5 临时性失败（自动重试）

- [ ] 制造暂时性错误（服务器返回 `IP地址不匹配` 类消息）→ `认证失败，将在 5 分钟后自动重试`，状态回「未连接」

### A6 在线保活与掉线自动重登

- [ ] 完成 A2 保持在线后，另开浏览器访问 `https://s.scut.edu.cn/drcom/logout?callback=dr1006` 主动下线
- [ ] 期望 ≤60s 内日志：`检测到 Portal 会话掉线，自动重新登录...` → `掉线后自动重新登录成功`，状态回到「已连接」
- [ ] 被动场景：长时间无流量被踢（SCUT 约 15 分钟）→ 同样自动重登

### A7 保活失败退避

- [ ] 断开网络（拔网线/断 WiFi）后观察日志：保活检测失败，下一轮检测周期应逐次翻倍（60s→120s→…，不产生刷屏）
- [ ] 恢复网络后：检测成功，保活周期复位到 60s

### A8 会话超时兜底

- [ ] 服务器完全不可达（填一个不存在的 host 或防火墙拦截 801 端口）→ 登录链路 30s 内应报 `认证超时（30 秒无响应）` 并进入自动重试排程，而非无限等待

### A9 断开注销

- [ ] 在线时点「断开」→ 日志 `正在注销 Portal 会话...` → `Portal 会话已注销` → 状态「未连接」
- [ ] 门户侧确认该 IP 已下线

### A10 自动化组合

- [ ] 勾选「启动后自动连接」+ 无线模式 → 重启程序自动认证成功
- [ ] `SCUTNetLogin.exe --silent` 静默启动 → 托盘自动认证

### A11 异常路径

- [ ] 停 WiFi / 断网后点连接 → `在线状态查询失败(...)尝试直接登录...` → 最终 `Portal 认证请求失败:`，进入重试排程；恢复网络后一个周期内自愈
- [ ] 多网卡 / VPN 存在时：`wlan_user_ip` 应取 Wi-Fi 接口 IP（登录日志中的 IP 应为校园网网段），而非 VPN/虚拟网卡 IP

---

## B. 有线 802.1X 回归（确保新功能未破坏旧链路）

### B1 手动连接

- [ ] 插宿舍网口，选有线网卡、账号密码 → 连接
- [ ] `开始802.1X认证...` → 认证成功 → `认证成功，可以上网了！`；UDP 心跳日志正常；状态「已连接」

### B2 静态 IP 流程

- [ ] 勾选「连接时配置静态IP」并填参数 → `正在设置静态IP: ...` → 认证
- [ ] 断开后确认网卡恢复 DHCP

### B3 断开 / 重连

- [ ] 断开 → 状态「未连接」
- [ ] （可选）拔网线再插，观察 5 分钟自动重连

### B4 认证超时兜底（本次新增）

- [ ] 交换机/服务器持续无响应（网线松、端口错误）→ 60s 内应报 `认证超时（60 秒无有效响应），即将自动重试` 并进入重连排程，而非永久卡在「正在认证」

---

## C. 常见问题速查

| 现象 | 处理 |
|---|---|
| 构建报 `pcap.h` 找不到 | `NPCAP_SDK` 环境变量未指向 `D:\Program Files (x86)\npcap-sdk` |
| nmake 报 `未找到 Qt6xxx` | Developer Command Prompt 中未 `set PATH=...Qt...\bin` |
| 无线连不上但日志停在查询 | 服务器地址错校区（换 s2）或 WiFi 未连 / 未拿到 IP |
| 提示「已在其他设备在线」 | SCUT 并发限制，先下线其他终端（A9 注销方法） |
| 有线「打开网卡失败」 | Npcap 未装或未勾 raw 802.1X；关闭其他占用网卡的抓包工具 |
| 运行缺 Qt DLL | 记得 `windeployqt release\SCUTNetLogin.exe` |
| 认证超时频繁触发 | 网络到认证服务器不通（801 端口被拦），检查防火墙/路由 |

---

## D. 通过标准

- [ ] A2 / A4 / A6 / A7 / A8 / A9 核心链路无异常通过
- [ ] 无线模式下 WLAN / 静态 IP 等控件确实禁用且状态正确
- [ ] 重启后 `authMode=wireless` 保持（A1）
- [ ] B1 有线 802.1X 正常（回归无损）
- [ ] 47 个单元测试全部通过（可选复查：`cd tests && qmake tst_packets.pro && nmake release && release\tst_packets.exe`）

---

## E. 测试记录

| 用例 | 结果（通过/失败/阻塞） | 备注 |
|---|---|---|
| A1 | | |
| A2 | | |
| A3 | | |
| A4 | | |
| A5 | | |
| A6 | | |
| A7 | | |
| A8 | | |
| A9 | | |
| A10 | | |
| A11 | | |
| B1 | | |
| B2 | | |
| B3 | | |
| B4 | | |

- 测试人：____________
- 日期：____________
- 校园网环境（校区/SSID/网口）：____________
- 结论：□ 通过　□ 有条件通过　□ 不通过（原因：____________）
